node_modules/
data/
*.log
.DS_Store

# secrets — never commit a real .env (the template .env.example stays tracked)
.env
.env.*
!.env.example

# per-app Fly config generated by a provisioning script; never commit one
.fly.*.toml
